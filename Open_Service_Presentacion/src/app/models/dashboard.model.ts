export interface StatCard {
  label: string;
  value: number;
  icon: string;
  color: string;
}

export interface RecentOrder {
  id: string;
  status: string;
  date: string;
  statusClass: string;
}
